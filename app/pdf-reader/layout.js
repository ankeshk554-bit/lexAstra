export const metadata = {
  title: 'AI PDF Assistant | LexAstra',
  description: 'Upload legal textbooks, bare acts, or reference documents, highlight and annotate, and chat with LexAstra AI using local smart RAG search context.',
};

export default function PDFReaderLayout({ children }) {
  return children;
}
